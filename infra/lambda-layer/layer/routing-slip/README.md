# routing-slip-pattern layer

Arquivos publicados em `/opt/routing-slip`.

- `config/config.yaml`: configuracao base para execucao em Lambda.
- `workflows/workflow.yaml`: substitua pelo workflow da solucao.

Preencha esta pasta com workflows, templates e configuracoes compartilhadas antes de publicar a layer.
